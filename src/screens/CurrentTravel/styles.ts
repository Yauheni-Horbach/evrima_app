import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerMap: {
    alignItems: 'center',
    height: 500,
    justifyContent: 'center',
    width: '100%',
  },
  map: {
    height: 400,
    width: 400,
  },
  placeItem: {
    borderColor: 'red',
    borderWidth: 1,

    height: 30,
    width: '100%',
  },
});
