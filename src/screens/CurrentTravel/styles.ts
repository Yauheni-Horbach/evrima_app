import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  map: {
    height: 300,
    width: 300,
  },
  containerMap: {
    height: 400,
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
