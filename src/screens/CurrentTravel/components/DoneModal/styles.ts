import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  doneButton: {
    backgroundColor: 'green',
    borderRadius: 5,
    marginTop: 20,
    padding: 10,
  },
  doneButtonText: {
    color: '#fff',
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
