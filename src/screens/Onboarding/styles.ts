import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  image: {
    height: 200,
    marginBottom: 20,
    width: 200,
  },
  subtitle: {
    fontSize: 16,
    marginBottom: 30,
    marginHorizontal: 40,
    textAlign: 'center',
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 10,
  },
});
