import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  alreadyUsed: {
    backgroundColor: 'red',
  },
  suggestionItem: {
    borderColor: 'black',
    borderWidth: 1,
    height: 40,
  },
});
